allOf:
  - $ref: "ErrorBase.yaml"
  - type: object
    properties:
      title:
        example: "Invalid Request"
      status:
        example: 400
      detail:
        example: "A request parameter is invalid."
